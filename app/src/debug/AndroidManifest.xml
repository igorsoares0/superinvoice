<?xml version="1.0" encoding="utf-8"?>
<!--
    Crash de desenvolvimento poluindo o painel faz a taxa de "crash-free users" mentir,
    que é justamente o número que se olha para decidir se dá para publicar. Este flag
    cobre a janela do primeiro start, antes de o CrashReporter aplicar o consentimento
    vindo do DataStore; a partir daí o valor persistido é quem manda.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
    </application>
</manifest>
